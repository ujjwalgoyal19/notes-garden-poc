---
description: Do one PR from the plan (next one, or /pr <number>)
argument-hint: "[PR number, e.g. 7 or 12a]"
---
Follow the "Every session: do exactly one PR" steps in CLAUDE.md.

Target PR: $ARGUMENTS (if empty, use "Next up" in TRACKER.md).

Start by stating the PR number, its title, and a 3-line summary of what you will change. Then do it, update TRACKER.md, and stop.
