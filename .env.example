# Server-side only (used by the Vite dev proxy, never shipped to the browser)
OPENROUTER_API_KEY=
# 1 = fake judge (word overlap), 0 = real Jev via OpenRouter
VITE_MOCK=1
# Pin once tuned; ~typesafe/jev-latest moves without notice
VITE_JEV_MODEL=typesafe/jev-1.13
